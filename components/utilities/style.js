import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        marginTop: 40,
        padding: 10,
        flex: 1,
        backgroundColor: 'red'
    },
    whiteText: {
        color: '#ffffff'
    }
})